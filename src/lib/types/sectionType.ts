export interface SkillBadgeData {
  label: string;
  src: string;
  colorBadges: string;
}
export interface ArticleItem {
  direction: "left" | "right";
  img: string;
  alt: string;
  paragraphs: string[];
}
